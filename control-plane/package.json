{
  "name": "@kb-platform/control-plane",
  "version": "0.1.0",
  "private": true,
  "description": "ArcadeDB KB platform control plane — registry, placement/router, provisioning, retrieval seam (INTERFACE STUBS for the CTO package).",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'eslint config added in Phase 2'",
    "test": "echo 'unit tests added in Phase 2'"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
